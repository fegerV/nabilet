# 2026-09-23

## Синхронизация с GitHub (origin/main)

Локальная ветка была на 25 коммитов позади `origin/main`, своих коммитов — 0.

- SSH: порт прокси в этой сессии — **55295**, прописан в `~/.ssh/proxy_port`.
  Проверка: `"C:/Program Files/Git/usr/bin/ssh.exe" -T git@github.com` → `Hi fegerV!`.
- `origin` переключён с HTTPS на **git@github.com:fegerV/nabilet.git** (HTTPS-пуш здесь не работает).
- **Ловушка:** после `git fetch` удалённые refs не создались — `.git/refs/remotes/` оказался
  пуст, `git branch -r` пустой, при этом `FETCH_HEAD` записан корректно. Создать refs через
  `git update-ref refs/remotes/origin/...` тоже не удалось (exit 0, но ref нет) — запись в
  подкаталог `.git/refs/remotes/origin` молча не проходит (mkdir виден в ls, но файл внутрь
  не пишется). **Обход: работать напрямую по SHA из `FETCH_HEAD`.**
- Мердж в foreground **убивается SIGTERM** на распаковке 6001 файлов node_modules
  (>2 мин). Мердж и `npm install` запускать только через `run_in_background`.
- После SIGTERM остаётся `.git/index.lock` → `rm -f .git/index.lock`.

Что было сделано:
1. Локальная работа (дизайн-система UI) сохранена коммитом `14b5150`.
2. Мердж `40bddd6` → конфликты в `package.json` (add/add), `routes/api.php`,
   `WebhookController.php` (add/add). Разрешены: package.json = объединение зависимостей;
   routes/api.php = Installer + Auth, убран дубль Events; WebhookController = взята
   рабочая версия с проверкой подписи ЮKassa. Коммит мерджа `7158f34`.
3. `node_modules` (6001 файлов, случайно закоммичен на сервере) снят с отслеживания —
   коммит `40eebde`. В архив для шаред-хостинга не попадёт.

## Архитектурный аудит (по спецификации заказчика)

Реализовано: инсталлятор (`app/Modules/Installer` — БД, админ, ключи ЮKassa,
`storage/install.lock`), `vite.config.js` → `outDir: public_html/build`,入口
`resources/js/app/{hall-editor,ticket-builder}.js`, домен холдов (`SeatHold` + `HoldSweeper`
с `lockForUpdate`), вебхук-контроллер, `docs/openapi.yaml`.

Критические пробелы (см. отчёт пользователю):
- **`seats:clear-expired` не существует** — `app/Console/Commands/` пуст, команда нигде не
  зарегистрирована. `routes/console.php` её вызывает → планировщик падает целиком,
  просроченные брони не снимаются. Сервис `HoldSweeper` есть — нужна обёртка-команда.
- **Нет пакетов** `yoomoney/yookassa-sdk-php`, `barryvdh/laravel-dompdf`,
  `simplesoftwareio/laravel-qrcode`, `laravel/sanctum` — при этом код уже вызывает
  `\PDF::loadView()` и `QrCode::format()` → runtime-ошибка.
- `QUEUE_CONNECTION=redis` в `.env.example` (на шаред-хостинге нужен `database`),
  нет миграции `jobs`, нет `queue:work --stop-when-empty`.
- Нет версионирования API (`prefix('v1')`), нет блокировки строки при check-in
  (гонка двойного прохода), Android — 2 файла-заглушки, `.github/workflows/android.yml` нет.
- В инсталляторе нет полей SMTP (MAIL_* есть только в `.env.example`).
- `config/filesystems.php` всё ещё `public_path('storage')`, а не `base_path('public_html/storage')`.

## Прочее

- npm: `caniuse-lite` был повреждён (225 файлов вместо ~835) — починен
  `npm install caniuse-lite@1.0.30001688 --no-save`; `npm install caniuse-lite@latest --force`
  пишет "up to date" и НЕ чинит.
- После мержа `vite.config.js` (пришёл с сервера) имеет приоритет над моим `vite.config.ts`;
  `npm run build` собирает продакшен-бандлы, `build:preview` — прототип дизайн-системы.
- Два редактора зала: `resources/js/components/HallEditor.vue` (с сервера) и
  `resources/js/pages/hall-editor/HallEditorPage.vue` (мой). Требует объединения.

## Запуск сервера и тестов (вторая половина дня)

**Окружение.** PHP: `C:/Users/Professional/AppData/Local/VertexCMS/tools/php/php.exe` (8.4.21).
PostgreSQL 18 стоит локально в `C:/Program Files/PostgreSQL/18`, уже слушает **5433**
(ровно как в `phpunit.xml`), auth `trust`, базы `nabilet` / `nabilet_testing`.
Роли `nabilet` не было — создал (`CREATE ROLE nabilet LOGIN SUPERUSER`).
Docker daemon **не запущен** (MySQL-контейнер `nabilet-mysql` недоступен).

**Запуск.** `php artisan serve --host=127.0.0.1 --port=8000` в фоне; `/up` → 200,
`/api/v1/ping` → `{"status":"ok"}`, `/` → 404 (корневого web-роута нет).
Тесты: `php artisan test` (только Feature) и `php tests/run.php` (Unit, без vendor).

**Ключевое открытие: влитый код не запускался ни разу.** `artisan` падал на
фатальной ошибке Filament, поэтому не работал и `schedule:run`.
Починено (коммит `c2477fc`): команда `seats:clear-expired` + регистрация в
`bootstrap/app.php`; три Filament-страницы (`$view` статический, базовый класс
`Filament\Resources\Pages\Page`); убраны ссылки на несуществующие `Reports`/`SystemLogs`;
порядок миграций (`event_content` перенесена в 2026_09_22_001500);
`OrderService::paginate()` fail-closed по `organization_id`;
тест `/api/ping` → `/api/v1/ping`.

**Результат:** Feature 5/5, Unit 639 методов / 0 падений.

**Главный нерешённый блокер:** модуль Orders написан под **архивную** схему
(`session_id`, `metadata`, `subtotal`, `tax_amount`), тогда как таблица `orders`
в миграциях — `subtotal_amount`, `fee_amount`, `payment_status`, `order_number`.
Путь записи заказов сломан. Плюс: почти все API-роуты публичны (только Payments
под `auth:api`), и набор `Unit` не подключён в `phpunit.xml`.

**Важно про artisan и провайдеры:** `bootstrap/providers.php` намеренно не
регистрирует `Nabilet\Core\NabiletServiceProvider`, поэтому провайдеры модулей
**не загружаются**. Регистрировать их целиком нельзя: каждый вызывает
`loadRoutesFrom()`, и роуты задублируются. Новые команды/биндинги модулей
регистрировать точечно в `bootstrap/app.php` (`->withCommands([...])`).
